ROOTDIR=$(realpath $(dir $(firstword $(MAKEFILE_LIST))))

PROJECTREQFILE=${ROOTDIR}/requirements.txt

SRCDIR=${ROOTDIR}/dexterous_bioprosthesis_2021_raw_datasets
TESTDIR?=${ROOTDIR}/tests
COVDIR=${ROOTDIR}/htmlcov_p
COVERAGERC=${ROOTDIR}/.coveragerc
REQ_FILE=${ROOTDIR}/requirements_dev.txt
INSTALL_LOG_FILE=${ROOTDIR}/install.log
VENV_SUBDIR=${ROOTDIR}/venv
COVERAGERC=${ROOTDIR}/.coveragerc
DOCS_DIR=${ROOTDIR}/docs
UML_DIR=${ROOTDIR}/docs/uml
SPHINX_SRCDIR=${ROOTDIR}/docs_sphinx
SPHINX_BUILDDIR=${ROOTDIR}/docs_sphinx/_build
SPHINX_APIDIR=${SPHINX_SRCDIR}/api
TOXDIR=${ROOTDIR}/.tox
DATADIR=${ROOTDIR}/data
STATICDIR=${ROOTDIR}/static_analysis
LINTFILE=${STATICDIR}/lint.json
FLAKE8FILE=${STATICDIR}/flake8.log
MYPYFILE=${STATICDIR}/mypy.log
TESTSETNAME=Andrzej_19_10_2022
TESTDATADIR=${DATADIR}/${TESTSETNAME}
TESTDATAZIP=${DATADIR}/${TESTSETNAME}.zip

UML_FORMAT ?= html
COVERAGE = coverage
UNITTEST_PARALLEL = unittest-parallel
PDOC= pdoc3
SPHINXBUILD= sphinx-build
SPHINXAPIDOC= sphinx-apidoc
PYREVERSE= pyreverse
DOT=neato
PYLINT= pylint
FLAKE8= flake8
MYPY= mypy
PYTHON=python
SYSPYTHON=python
#--system-site-packages
VENV_OPTIONS=
PIP=pip
PYTEST=pytest
TOX=tox

VENV_BACKEND ?= ${SYSPYTHON} -m venv
VENV_FLAGS ?= --upgrade-deps
PIP_CMD ?= ${PYTHON} -m ${PIP}
PIP_INSTALL_FLAGS ?= --prefer-binary --log ${INSTALL_LOG_FILE}

TOX_CORES=auto
LOGDIR=${ROOTDIR}/testlogs
LOGFILE=${LOGDIR}/`date +'%y-%m-%d_%H-%M-%S'`.log


ifeq ($(OS),Windows_NT)
	ACTIVATE:=. ${VENV_SUBDIR}/Scripts/activate
else
	ACTIVATE:=. ${VENV_SUBDIR}/bin/activate
endif

.PHONY: all clean test docs

all:profile 

clean: clean_pypackages clean_venv clean_tox
	@echo "Cleaning up build artifacts, virtual environments, and test logs..."

clean_pypackages:
	rm -rf pypackages

clean_venv:
	rm -rf ${VENV_SUBDIR} ${INSTALL_LOG_FILE} ${LOGDIR}

clean_tox:
	rm -rf ${TOXDIR}

venv:
	${VENV_BACKEND} ${VENV_FLAGS} ${VENV_OPTIONS} ${VENV_SUBDIR}
	${ACTIVATE}; ${PIP_CMD} install wheel setuptools pypackages

pypackages: venv
	${ACTIVATE}; ${PIP_CMD} install -e ${ROOTDIR} ${PIP_INSTALL_FLAGS} -r ${REQ_FILE}
	touch $@

test: pypackages data_unp

	mkdir -p ${LOGDIR}
	${ACTIVATE}; ${COVERAGE} run --branch  --source=${SRCDIR} -m unittest discover -p '*_test.py' -v -s ${TESTDIR} 2>&1 |tee -a ${LOGFILE}
	${ACTIVATE}; ${COVERAGE} html --show-contexts

test_parallel: pypackages data_unp

	mkdir -p ${COVDIR}  ${LOGDIR}
	${ACTIVATE}; ${UNITTEST_PARALLEL} -v -t ${ROOTDIR} -s ${TESTDIR} -p '*_test.py' --coverage --coverage-rcfile ./.coveragerc --coverage-source ${SRCDIR} --coverage-html ${COVDIR} 2>&1 |tee -a ${LOGFILE}

docs: pypackages uml
	${ACTIVATE}; $(PDOC) --force --html ${SRCDIR} --output-dir ${DOCS_DIR}

sphinx: pypackages uml
	@rm -rf ${SPHINX_APIDIR}
	${ACTIVATE}; ${SPHINXAPIDOC} -f -o ${SPHINX_APIDIR} ${SRCDIR}
	@# Copy UML SVGs into Sphinx static assets
	@rm -rf ${SPHINX_SRCDIR}/_static/uml
	@mkdir -p ${SPHINX_SRCDIR}/_static/uml
	@cp -r ${UML_DIR} ${SPHINX_SRCDIR}/_static/uml
	@# Auto-generate uml.rst with all discovered SVGs
	@echo "UML Diagrams" > ${SPHINX_SRCDIR}/uml.rst
	@echo "============" >> ${SPHINX_SRCDIR}/uml.rst
	@echo "" >> ${SPHINX_SRCDIR}/uml.rst
	@echo "UML class and package diagrams generated by \`\`pyreverse\`\` and \`\`graphviz\`\`." >> ${SPHINX_SRCDIR}/uml.rst
	@echo "" >> ${SPHINX_SRCDIR}/uml.rst
	@find ${SPHINX_SRCDIR}/_static/uml -name '*.svg' | sort | while read -r svg; do \
		rel="$${svg#$(SPHINX_SRCDIR)/}"; \
		label="$$(echo "$$rel" | sed 's|_static/uml/||;s|/| — |g;s|\.svg||')"; \
		echo "$$label" >> ${SPHINX_SRCDIR}/uml.rst; \
		printf '%*s\n' "$${#label}" '' | tr ' ' '-' >> ${SPHINX_SRCDIR}/uml.rst; \
		echo "" >> ${SPHINX_SRCDIR}/uml.rst; \
		echo ".. raw:: html" >> ${SPHINX_SRCDIR}/uml.rst; \
		echo "" >> ${SPHINX_SRCDIR}/uml.rst; \
		echo "   <div class=\"uml-container\">" >> ${SPHINX_SRCDIR}/uml.rst; \
		echo "     <div class=\"uml-toolbar\">" >> ${SPHINX_SRCDIR}/uml.rst; \
		echo "       <a href=\"$$rel\" target=\"_blank\" title=\"Open in new tab for full zoom\">&#x1F50D; Open full size</a>" >> ${SPHINX_SRCDIR}/uml.rst; \
		echo "     </div>" >> ${SPHINX_SRCDIR}/uml.rst; \
		echo "     <object type=\"image/svg+xml\" data=\"$$rel\" style=\"width:100%;min-height:300px;\">SVG not supported</object>" >> ${SPHINX_SRCDIR}/uml.rst; \
		echo "   </div>" >> ${SPHINX_SRCDIR}/uml.rst; \
		echo "" >> ${SPHINX_SRCDIR}/uml.rst; \
	done
	${ACTIVATE}; ${SPHINXBUILD} -b html ${SPHINX_SRCDIR} ${SPHINX_BUILDDIR}

uml: pypackages
	@echo "Generating UML diagrams..."
	@rm -rf "$(UML_DIR)"
	@mkdir -p "$(UML_DIR)"

	@${ACTIVATE}; find "$(SRCDIR)" -type f -name '__init__.py' | \
	while read -r init; do \
		pkg="$$(dirname "$$init")"; \
		rel="$${pkg#$(SRCDIR)}"; \
		rel="$${rel#/}"; \
		name="$$(basename "$$pkg")"; \
		outdir="$(UML_DIR)/$$rel"; \
		tmpdir="$$(mktemp -d)"; \
		mkdir -p "$$outdir"; \
		echo "  $$pkg"; \
		\
		$(PYREVERSE) \
			-o dot \
			-p "$$name" \
			-d "$$tmpdir" \
			"$$pkg"; \
		\
		if [ -f "$$tmpdir/classes_$$name.dot" ]; then \
			$(DOT) \
				-Tsvg \
				"$$tmpdir/classes_$$name.dot" \
				-o "$$outdir/classes.svg"; \
		fi; \
		\
		if [ -f "$$tmpdir/packages_$$name.dot" ]; then \
			$(DOT) \
				-Tsvg \
				"$$tmpdir/packages_$$name.dot" \
				-o "$$outdir/packages.svg"; \
		fi; \
		\
		rm -rf "$$tmpdir"; \
	done


profile: pypackages data_unp

	${ACTIVATE}; ${PYTEST} -n auto --cov-report=html --cov=${SRCDIR} --profile ${TESTDIR}

${STATICDIR}:
	mkdir -p ${STATICDIR}
flake8: pypackages ${STATICDIR}
	${ACTIVATE}; ${FLAKE8} --jobs auto ${SRCDIR} > ${FLAKE8FILE} || true

mypy: pypackages ${STATICDIR}
	${ACTIVATE}; ${MYPY} --pretty --show-error-context ${SRCDIR} > ${MYPYFILE} || true
lint: pypackages ${STATICDIR}
	${ACTIVATE}; ${PYLINT} -j 0 ${SRCDIR} --output-format=json > ${LINTFILE} || true

static_check: flake8 mypy lint

${TESTDATADIR}:
	@echo "Unpacking ${TESTDATAZIP}"
	unzip ${TESTDATAZIP} -d ${DATADIR}

data_unp: ${TESTDATADIR}
	@echo "Creating data"

tox_check: pypackages
	${ACTIVATE}; ${TOX} -p ${TOX_CORES} 